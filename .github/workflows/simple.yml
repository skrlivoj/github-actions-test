name: Shell commands

on:
  - push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello"
      - name: multiline script
        run: |
          date
          ping -v
        
  