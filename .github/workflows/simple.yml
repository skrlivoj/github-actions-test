name: Shell commands

on:
  - push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: no.1 echo a string
        run: echo "Hello"
      - name: no.2 multiline script
        run: |
          date
          pwd
          ls -la
        
  